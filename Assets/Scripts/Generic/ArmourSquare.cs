using UnityEngine;
using System.Collections;

public class ArmourSquare : Square {
	
	public ArmourSquare(float mass, float health, string leftUUID, string rightUUID, string upUUID, string downUUID) : base (mass, health, leftUUID, rightUUID, upUUID, downUUID){

	}
}
