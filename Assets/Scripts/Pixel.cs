using UnityEngine;
using System.Collections;

public class Pixel
{
    /// <summary>
    /// Stores one enumerator to define pixel types. Prevents conversations being needed between other classes.
    /// </summary>

    //Define pixel types.
    public enum Type { Core, Scrap, Armour, Power, Engine, Hardpoint };

}