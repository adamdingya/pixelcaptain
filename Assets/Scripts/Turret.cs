using UnityEngine;
using System.Collections;

public class Turret
{
    /// <summary>
    /// Stores one enumerator to define turret types. Prevents conversations being needed between other classes.
    /// </summary>

    //Define turret types.
    public enum Type { None, Small, Medium, Large, Laser};
}
